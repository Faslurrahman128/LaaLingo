class WritingBrain {
  Future<void> extractTextFromImage(String imagepath) async {
    // Temporarily disabled: the previous implementation used an outdated
    // google_vision API that no longer matches the package version.
    // Re-implement OCR here if/when needed.
    return;
  }
}
